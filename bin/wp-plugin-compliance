#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
export WP_PLUGIN_COMPLIANCE_RUNNER="${WP_PLUGIN_COMPLIANCE_RUNNER:-wp-plugin-compliance}"

exec node "$REPO_ROOT/src/cli.js" "$@"
